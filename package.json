{
  "name": "photo-land",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "install": "cd api && npm i && cd ../client && npm i",
    "start": "concurrently \"cd api && npm start\" \"cd client && npm start\""
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
